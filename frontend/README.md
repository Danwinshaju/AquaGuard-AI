# AquaGuard frontend

This React, TypeScript, Vite, Tailwind CSS, React Router, and TanStack Query application gives beginners a simple upload-and-analyse workflow. Vite proxies `/api` requests to FastAPI at port 8000 during local development.
